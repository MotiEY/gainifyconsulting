<section class="ai-section" id="ai-approach" aria-label="AI בעסק שלך">
  <div class="ai-inner">
    <span class="tag reveal">AI בעסק שלך</span>

    <p class="ai-lead reveal">
      AI הוא כבר לא שאלה של אם.<br />
      השאלה היא איפה, למה ואיך מודדים.
    </p>

    <ul class="ai-list reveal" role="list">
      <li>איזה צורך עסקי הוא אמור לשרת</li>
      <li>איזה תהליך צריך להשתנות</li>
      <li>מי אמור לעבוד או להחליט אחרת</li>
      <li>איך נדע שזה באמת יצר ערך</li>
    </ul>

    <p class="ai-body reveal">AI יכול לעזור כמעט לכל עסק: להוריד עומס, לשפר שירות, לקצר עבודה ידנית, לייצר תמונת מצב טובה יותר, או לפתוח יכולת חדשה. אבל כשהוא נכנס בלי חשיבה מקדימה, בלי תהליך ברור ובלי מדדי הצלחה, הוא נשאר עוד כלי שלא באמת משנה את העסק.</p>

    <div class="ai-quote reveal">
      <p>AI שעובד מתחיל מצורך עסקי ונמדד בתוצאה.</p>
    </div>
  </div>
</section>

<style>
  .ai-section {
    padding: 72px 24px;
    border-top: 1px solid var(--line);
    background: var(--cream);
  }

  .ai-inner {
    max-width: var(--read-w);
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 28px;
  }

  .ai-lead {
    font-family: 'Frank Ruhl Libre', serif;
    font-weight: 700;
    font-size: clamp(20px, 4.5vw, 28px);
    line-height: 1.4;
    color: var(--dark);
    letter-spacing: -0.02em;
  }

  .ai-list {
    list-style: none;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0;
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
  }

  .ai-list li {
    font-family: 'Assistant', sans-serif;
    font-size: clamp(15px, 3vw, 17px);
    line-height: 1.7;
    color: var(--mid);
    padding: 12px 0;
    border-bottom: 1px solid var(--line);
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .ai-list li:last-child { border-bottom: none; }

  .ai-list li::before {
    content: '';
    display: inline-block;
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: var(--bronze);
    flex-shrink: 0;
  }

  .ai-body {
    font-family: 'Assistant', sans-serif;
    font-size: clamp(16px, 3vw, 17px);
    line-height: 1.88;
    color: var(--mid);
  }

  .ai-quote {
    background: var(--cream-3);
    border-inline-end: 3px solid var(--bronze);
    padding: 20px 24px;
    border-radius: 4px;
  }

  .ai-quote p {
    font-family: 'Frank Ruhl Libre', serif;
    font-weight: 400;
    font-style: italic;
    font-size: clamp(16px, 3.5vw, 18px);
    line-height: 1.65;
    color: var(--bronze);
    margin: 0;
  }

  @media (min-width: 768px) {
    .ai-section { padding: 90px 40px; }
    .ai-list li { padding: 14px 0; }
  }

  @media (min-width: 1280px) {
    .ai-section { padding: 110px 52px; }
    .ai-lead { font-size: clamp(22px, 2vw, 28px); }
  }
</style>
